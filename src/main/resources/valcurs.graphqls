type Query {
    getAllCurrencyInfo: [CurrencyInfo]
    getCurrencyConversion (fromId: String, toId: String, amount: Float): Float
}

type CurrencyInfo {
    numCode: Int
    charCode: String
    name: String
    id: String
}
